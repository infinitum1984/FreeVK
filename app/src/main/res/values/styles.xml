<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/VkFree</item>
        <item name="colorPrimaryDark">@color/VkFree</item>
        <item name="colorAccent">@color/VkFree</item>
        <item name="android:textColorPrimary">?attr/colorPrimary</item>
        <item name="android:colorBackground">@color/VkFreeBlack</item>
        <item name="android:windowBackground">@android:color/background_dark</item>
    </style>

</resources>
